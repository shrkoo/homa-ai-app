plugins {
    id 'com.android.application'
}

android {
    namespace 'ai.homa.app'
    compileSdk 35

    defaultConfig {
        applicationId "ai.homa.app"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }

        debug {
            minifyEnabled false
        }
    }
}
